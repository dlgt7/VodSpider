================================================================================
                    XBPQ 影视源 Spider 使用说明
================================================================================

XBPQ 是 CatVodSpider（VodSpider）框架的影视爬虫，以 && 正则截取为核心规则语法，
支持中英文双键配置（XBPQKey 统一登记）、CSS/Jsoup 选择器模式、多线聚合、
Cloudflare/宝塔反爬绕过、图片代理、弹幕加载、M3U8 解析、{{变量}} 模板、
动态域名切换、镜像域名、SSRF 防护等特性。

本文档基于 com.github.catvod.spider.XBPQ.java 与 XBPQKey.java 源码整理，
与代码实现逐一核对，涵盖类结构、方法说明、配置项总览、规则语法与注意事项。

================================================================================
一、类结构与模块划分
================================================================================

本项目为单文件实现 + 键注册表，全部逻辑位于两个类：

1. com.github.catvod.spider.XBPQ（主类，继承 com.github.catvod.crawler.Spider）
   内部静态类/工具：
   ├─ HtmlMatchInfo        HTML 命中信息（标签区间上下文）
   ├─ HtmlNodeHelper       HTML 节点提取辅助
   ├─ JsoupExtractor       Jsoup/CSS 选择器提取（css:/css:// 前缀分流）
   ├─ CssRuleInfo          CSS 规则信息
   ├─ RuleUtils            规则工具（getRegion 区域截取等）
   ├─ NodeExtractionResult 节点提取结果
   ├─ DetailExtractionContext 详情提取上下文
   ├─ SearchFetchResult    搜索抓取结果（content + url）
   └─ SearchNodeResult     搜索节点结果

2. com.github.catvod.spider.XBPQKey（字段命名标准，全项目单一事实源）
   ├─ TABLE 注册表：k(模块, 规范英文键, 规范中文名, 别名...)
   ├─ norm(key)：任意写法 → 规范英文键（未登记原样透传）
   ├─ aliasMap()：非规范写法 → 规范键映射（规则加载时一次性归一化）
   └─ isKnown/cn/module/dump：登记查询与文档输出

3. 关键机制
   ├─ 键归一化：CHINESE_KEY_MAP = XBPQKey.aliasMap()，fetchRule() 时
   │    convertChineseKeys → normalizeRuleKeys 两条链路统一收敛到规范英文键
   ├─ 网络层：OkHttp（com.github.catvod.utils.OkHttp）+ 反爬检测
   │    （isCloudflarePage/bypassCloudflare、isBaoTaWafPage/btwaf cookie 提取）
   ├─ Cookie 合并：Util.mergeCookies（滑块验证后回写规则头）
   └─ 日志：com.github.catvod.crawler.SpiderDebug

================================================================================
二、核心方法说明（@Override）
================================================================================

1. init(Context context, String extend)
   ──────────────────────────────
   初始化入口。extend 支持：
   - http://...json  远程规则文件（fetchRule 懒加载 + 双重检查锁定）
   - clan://...json  本地路径
   - 直接 JSON 字符串  内联配置
   fetchRule() 将 JSON 经键归一化后缓存到 rule 字段，并执行：
   applyDynamicDomain()（动态域名切换）→ 初始化变量表 variableMap。

2. homeContent(boolean filter) → {"class":[...], "filters":{...}}
   ───────────────────────────────
   首页分类构建优先级（buildClassList）：
     0) tryBuildFromJson：cat_mode=0 的分类 JSON 模式（catjsonlist 等）
     1) fenlei 显式配置存在时优先（含 $ 的 "名$值#名$值"）
     2) cateManual 分节对象 / guessCateManualIfNeeded 自动猜测分类
     3) cat_twice 二次截取后 guessRuleCateManual 猜测
     4) class_name + class_value 并行 & 数组
     5) cat_array + cat_title + cat_id 从首页 HTML 提取
     6) tryBuildFromFenlei（fenlei $ 格式）
     7) fallbackClassBuild：fenlei 整串作分类名 + extractCateId(class_url)
   后处理：cate_whitelist 白名单过滤 → insertActionTabs 功能入口插入。
   filter=true 且筛选非空时返回 filters（filter 内联 > filterdata URL 抓取，
   抓取后按 f* 替换组改写；sort_type/sort_value 自动生成 by 排序筛选器）。

3. homeVideoContent() → {"list":[...]}
   ───────────────────────────────
   首页推荐聚合：firstpage 纯数字=每分类条数上限；"名$ID#名$ID"=指定分区；
   hot_recommend=1 无 firstpage 也聚合。跨分类去重（vod_id 优先）。

4. categoryContent(String tid, String pg, boolean filter, HashMap extend)
   ──────────────────────────────────────────────────────────────────────────
   分类列表页。
   - 页码：startpage 起始偏移；offset_paging=1 时 {offset}=(页码-1)*page_size
   - 占位符：{cateId}/{catePg}（{catepg} 小写兼容）
   - 列表模式：网页截取（list_array 等）/ JSON 模式（list_mode + listjson* 字段组）
   - skip_bad_item=1（默认）单条失败跳过；"0"=整页失败
   - 后处理：图片净化（img_attr 懒加载属性/default_pic 兜底）→ 标题清洗
     → filter_word 过滤词剔除 → reverse 倒序 → page_end_marker 终止翻页
   - 页总数：list_total 规则提取 / 共N页 共N条 x/y页猜测，兜底满员 2 空 1
   - 失败重试：mirror_hosts 镜像域名逐个切换；url_append 全局参数追加

5. detailContent(List<String> ids) → {"list":[{vod字段...}]}
   ───────────────────────────────
   - vid 解码：encodeVodId 的 Base64 JSON 自动还原（兼容历史 {"vod_id":vid}）
   - detail_url 模板：先 ${vid} 再 {vid}，兜底 homeUrl 主机拼接
   - 字段提取：CSS 模式（detail_* 规则含 css: 前缀）或 && 截取
   - hexEscapeDecode：链接中 %uXXXX JS 转义解码
   - detail_separator 从纯文本按 label 补缺（导演：/主演：等自动映射 vod_*）
   - detail_merge 将指定 vod_* 字段换行追加到 vod_content（仅增量）
   - 线路：from_array/line_second_cut 提取 → line_name_map 重命名
     → episode_filter 过滤 → episode_sort/episode_dedup 排序去重
     → episode_quality 清晰度优先 → epiurl_prefix/epiurl_suffix 前后缀
     → merge_lines 合并 → empty_play_url 空兜底
   - auto_maccms=1 字段缺失注入 Maccms 默认规则；
     Anal_MacPlayer=2 优先正则解析 MacPlayer 脚本块

6. searchContent(String keyword, boolean quick, String pg)
   ─────────────────────────────────────────────
   - 配置缺失时 guessSearchUrlIfNeeded 自动猜测搜索接口
   - 扁平 search_url：{wd} 自动 URLEncoder.encode，{pg} 经 sea_firstpage 偏移
     （分节 search.url 写法 {wd} 原样替换）
   - search_word_sep 关键词空格替换（如 "+"）
   - searchjson* JSON 字段组优先；search_mode=0 JSON 优先 / =1 网页优先，互为兜底
   - JSON 自动探测：parseJsonSearchResult 递归查找同时含配置 id/name 字段的数组
   - CSS 模式（搜索规则含 css:）→ 传统 && 截取，无结果自动互切
   - quick 参数当前被忽略（快搜与普搜行为一致）

7. playerContent(String flag, String url, List<String> vipFlags)
   ─────────────────────────────────────────────────────────────
   播放解析，按优先级：
     1) isVideoUrl(url) → directResult（parse=0，直链直出）
     2) force_play=1/2 → 直接播放（分类/详情链接直出，可配 play_suffix）
     3) jump_url 非空 → tryJumpPlay（抓取播放页提取真实地址）→ 失败则嗅探
     4) manualVideoCheck=1 → directResult（免嗅直出）
     5) 否则 → buildSniffResult（parse=1，播放器嗅探）
   - play_header JSON 注入 result.header；play_prefix/play_suffix 前后缀
   - play_strip_params=1 播放链接剥离 query
   - danmuUrl 非空注入 danmaku 代理参数
   - isVideoUrl：video_format 嗅探词命中或默认扩展名；video_filter 命中强制非视频

8. proxy(Map<String,String> params)
   ─────────────────────────────────
   proxy:// 总入口分发：
   - do=XBPQ&danmu_url=... → loadDanmu 拉取弹幕，JSON 转 B 站风格 XML
   - M3U8 参数 → doLoadM3u8 拉取内容，相对路径补全，EXTM3U/EXTINF 探测
   - pic 参数 → loadPic 图片回源（secretKey MD5 签名校验）
   - 所有回源强制 SSRF 防护，不受 allow_internal 影响

9. manualVideoCheck() / isVideoFormat(url)
   将 免嗅/嗅探词/视频过滤词 同步给框架嗅探决策，与 playerContent 一致。

10. isFail(int code) / errorCodes / failCodes / successCodes
    响应 JSON code 判定（失败码+错误码 命中即失败；成功码白名单优先）。

================================================================================
三、配置项总览（按 XBPQKey 模块，规范中文名 | 规范英文键 | 别名）
================================================================================

【BASE 基础信息】
  站名 siteName | 作者 author | 主页url homeUrl | home_url, 主页, baseUrl
  请求头 header | headers | 公共请求头 headerJson | 公共头 | User userHeader | user
  UserAgent User-Agent | ua | Referer referer | 编码 encoding | charset
  超时 timeout | 重试 retries | retry | 起始页 startpage | startPage
  首页 firstpage | homePage | 内网放行 allow_internal | allowInternal
  平台 platform | 平台类型 | 后缀 domainSuffix | 域名后缀
  域名-c dynamic_domain | 动态域名 | 主页url-c home_url_c | 备用主页
  打开调试 openDebug | debug | 偏好菜单 prefMenu | 功能入口 actionTabs
  反爬超时 antiCrawlTimeout | 错误码 errorCodes | 失败码 failCodes | 成功码 successCodes
  宝塔WAF btwaf | 请求间隔 request_interval | requestInterval, 限流间隔
  镜像域名 mirror_hosts | mirrorHosts, 备用域名 | 参数追加 url_append | urlAppend, 全局参数
  翻页终点标记 page_end_marker | pageEndMarker, 分页结束标记
  偏移分页 offset_paging | offsetPaging | 每页条数 page_size | pageSize
  默认封面 default_pic | defaultPic
  滑块Key接口 js_key_url | 打码接口 ocr_api | 验证类型 verify_type
  WebView渲染 web_view | webview（仅注册，引擎告警"未实现"并忽略）
  WebView等待 web_view_wait | WebView就绪标记 web_view_ready
  WebView就绪超时 web_view_ready_timeout（同上，均未实现）

【CATEGORY 分类】
  分类url class_url | cateUrl, list_url | 分类 fenlei | cate
  分类名称 class_name | cateName | 分类值 class_value | cateValue
  分类二次截取 cat_twice | cateTwice | 分类数组 cat_array | cateArray
  分类标题 cat_title | cateTitle | 分类ID cat_id | cateId
  特殊分类链接 special_cate（仅注册，引擎不消费）
  筛选 filter | 分类筛选 | 筛选数据 filterdata | 分类截取模式 cat_mode
  分类JSON列表 catjsonlist | 分类JSON名称 catjsonname | 分类JSONID catjsonid
  分类JSON图片 catjsonpic | 分类JSON状态 catjsonnote
  筛选子分类名称 fclass_name | 筛选子分类替换词 fclass_value
  筛选类型名称 fcatelog_name | 筛选类型替换词 fcatelog_value
  筛选地区名称 farea_name | 筛选地区替换词 farea_value
  筛选年份名称 fyear_name | 筛选年份替换词 fyear_value
  筛选语言名称 flang_name | 筛选语言替换词 flang_value
  筛选排序名称 fsort_name | 筛选排序替换词 fsort_value
  排序 sort_type | sortType | 排序值 sort_value | sortValue
  热门推荐 hot_recommend | 列表显示 list_display | 分类白名单 cate_whitelist

【LIST 列表】
  数组 list_array | 二次截取 list_twice | 列表二次截取
  标题 list_name | 链接 list_id | 图片 list_pic
  副标题 list_remarks | 分类副标题, cat_subtitle
  链接前缀 list_prefix | 分类片单链接前缀, cat_prefix
  链接后缀 list_suffix | 分类片单链接后缀, cat_suffix
  过滤词 filter_word | 倒序 reverse | 倒序播放 | 列表模式 list_mode
  列表JSON路径 listjsonlist | 列表JSON名称 listjsonname | 列表JSONID listjsonid
  列表JSON图片 listjsonpic | 列表JSON状态 listjsonnote
  坏条目跳过 skip_bad_item | skipBadItem
  列表总页数规则 list_total | listTotal, 总页数, 总页数规则, 总数规则

【SEARCH 搜索】
  搜索url search_url | 搜索模式 search_mode | 搜索数组 search_array
  搜索标题 search_name | 搜索图片 search_pic | 搜索链接 search_id
  搜索副标题 search_remarks | 搜索简介 search_content | 搜索二次截取 search_twice
  搜索请求头 search_header | 搜索链接前缀 search_prefix
  搜索链接后缀 search_suffix | 搜索后缀 | 搜索起始页码 sea_firstpage | 搜索起始页
  搜索JSON路径 searchjsonlist | 搜索JSON名称 searchjsonname | 搜索JSONID searchjsonid
  搜索JSON图片 searchjsonpic | 搜索JSON状态 searchjsonnote
  搜索词分隔符 search_word_sep | searchWordSep, 关键词分隔符

【DETAIL 详情】
  详情url detail_url | 详情数组 detail_array | 详情二次截取 detail_twice
  影片名称 detail_name | 影片类型 detail_type | 类型 | 影片年代 detail_year | 年份
  影片地区 detail_area | 地区 | 导演 detail_director | 主演 detail_actor
  简介 detail_content | 剧情 | 状态 detail_remarks | 播放图片 play_image
  详情分隔符 detail_separator | detail_label_split
  详情合并字段 detail_merge | detail_content_merge

【PLAY 线路与播放】
  线路数组 from_array | 线路标题 from_title | 线路二次截取 line_second_cut | line_twice
  播放数组 play_array | 播放列表 url_array | 播放标题 url_title | 播放链接 url_url
  播放二次截取 play_twice | 多线数组 multi_line_array
  多线链接 multi_line_url | 线路链接 | 多线二次截取 multi_line_twice
  多线链接前缀 multi_line_prefix | 多线链接后缀 multi_line_suffix
  线路合并 merge_lines | 剧集过滤 episode_filter
  空播放兜底 empty_play_url | 空播放线路名 empty_play_from
  选集链接加前缀 epiurl_prefix | 选集链接加后缀 epiurl_suffix
  直接播放 force_play | 播放链接前缀 play_prefix | 播放链接后缀 play_suffix
  播放请求头 play_header | 跳转播放链接 jump_url
  分析MacPlayer Anal_MacPlayer | 自动Maccms auto_maccms
  剧集排序 episode_sort | episodeSort, 选集排序
  剧集去重 episode_dedup | episodeDedup, 选集去重
  线路重命名 line_name_map | lineNameMap, 线路映射
  播放去参数 play_strip_params | playStripParams, 剥离参数
  清晰度优先 episode_quality | episodeQuality, 剧集清晰度, 画质优先

【SNIFF 嗅探】
  嗅探词 video_format | 视频过滤词 video_filter | 免嗅 manualVideoCheck

【PROXY 图片代理/弹幕/直播】
  图片代理 PicNeedProxy | 图片代理前缀 baseEncodeUrl | 代理密钥 secretKey
  图片代理白名单 pic_allow_domains | picAllowDomains, 代理白名单, 代理域名白名单
  图片属性 img_attr | imgAttr, 懒加载属性, 图片真实地址属性
  弹幕url danmuUrl | 直播url liveUrl（仅注册，引擎不消费）

【CSS（Jsoup）选择器】
  CSS 选择器 css_selector | CSS选择器 | CSS 提取 css_extract | CSS提取
  CSS 属性 css_attribute | CSS属性 | JSOUP 解析 jsoup_parse | JSOUP解析
  列表CSS容器 list_css_container

================================================================================
四、规则语法详解
================================================================================

────────────────────────────────────────────────────────────────
1. && 正则截取（核心语法）
────────────────────────────────────────────────────────────────

格式："前缀&&后缀" → 截取前缀与后缀之间的内容
（正则转义后 DOTALL 非贪婪匹配；命中多处时全部提取，如列表数组）

示例：
  "title=\"&&\""          → 截取 title="..." 中的值
  "href=\"&&\""           → 截取 href="..." 中的值
  ">&&<"                  → 截取标签内文本
  "类型:&&<br"            → 截取 "类型:" 后到 "<br" 的内容
  "A&&B&&C&&D"            → 四段导航：先取 A..B，再在其中取 C..D

────────────────────────────────────────────────────────────────
2. || 备用选择器
────────────────────────────────────────────────────────────────

"规则1||规则2||规则3" → 依次尝试，取第一个非空结果（applyOrSelector）

────────────────────────────────────────────────────────────────
3. CSS 选择器模式（css:/css:// 前缀）
────────────────────────────────────────────────────────────────

由 isCssModeEnabled 判断：列表/详情/搜索/线路规则含 css: 前缀时启用，
JsoupExtractor 执行提取；CSS 无结果自动回退传统 && 截取。

  "css:.stui-vodlist__box"    → 类选择器
  "css:#id"                   → ID 选择器
  "css:.title->text"          → 取文本
  "css:a->attr(href)"         → 取属性
  "css:.desc->html"           → 取 HTML

────────────────────────────────────────────────────────────────
4. 后处理器标记（applyPostProcessors）
────────────────────────────────────────────────────────────────

值末尾追加标记，提取后执行：
  [替换:旧>>新#旧>>新]  → 文本替换，# 分隔多组；旧串含 * 时转 .* 正则通配
  [包含:关键词]         → 包含关键词才保留，否则返回空
  [不包含:关键词]       → 不含关键词才保留，否则返回空

────────────────────────────────────────────────────────────────
5. {{变量}} 模板替换（resolveVariables）
────────────────────────────────────────────────────────────────

支持最多 10 轮递归展开。内置变量（variableMap）：
  {{主页url}}   → homeUrl 配置值
  {{站名}}      → siteName 配置值
  {{作者}}      → author 配置值
  {{分类url}}   → class_url 配置值
  {{搜索url}}   → search_url 配置值
  {{后缀}}      → domainSuffix 配置值
  {{密钥}}      → secretKey 配置值
  {{域名-c}}    → 动态域名链解析结果（无效时回退 homeUrl）
  {{主页url-c}} → 主页备选解析结果（无效时回退 homeUrl）

示例：
  "搜索url": "{{主页url}}/search.php?wd={wd}&page={pg}"

────────────────────────────────────────────────────────────────
6. 分节规则写法
────────────────────────────────────────────────────────────────

除扁平键外支持子对象写法（加载时自动归一化为扁平键）：
  "cateManual": {"电影":"1","电视剧":"2"}   → 分类映射
  "search": {"url":"...","vod_id":"id","vod_name":"name"}
      → 分节搜索（vod_id 等的值 = JSON 数据字段名）
注意：分节 search.url 的 {wd} 原样替换；扁平 搜索url 的 {wd} 自动 URL 编码。

────────────────────────────────────────────────────────────────
7. 本版不存在的写法（避免误用）
────────────────────────────────────────────────────────────────

  [工具:xxx] 工具链（源码/重定向/解url/解b64/SHA/AES 等）→ 未实现
  [firstPage=...] URL 中括号首页语法 → 未实现（首页聚合用 首页/firstpage 键）
  搜索 URL 末尾 ";post" → postSearch 已定义但未接入调用链，当前无效
  quick 快搜过滤 → quick 参数被忽略，快搜与普搜行为一致

================================================================================
五、方法行为详解（含源码计算规则）
================================================================================

────────────────────────────────────────────────────────────────
1. wrapList 页总数计算链路
────────────────────────────────────────────────────────────────
categoryContent 最终调用 wrapList(videos, body, pg) 构造返回列表。
计算顺序如下（一旦命中即停止）：

  ① videos 为空（size=0）→ pagecount = max(0, pageNum-1)，total=0

  ② list_total 非空 → 解析为整数 total，pagecount = ceil(total/size)

  ③ list_total 为空 但 body（原始 HTML）非空 → 调用 parseTotalFromHtml(body)：
       a) parseTotalUniversal(body)：
          - JSON "total":N / "count":N → 直接返回 N
          - JSON {varName}:N，varName ∈ TOTAL_VAR_NAMES：
            若 varName 是页数变量（mac_page/pages/pagecount/totalpage 等），
            返回 N*20；否则直接返回 N
          - 「共 N 条」文本匹配 → 返回 N
          - 「共 N 页」文本匹配 → 返回 N
       b) parseIntFromScript(html, "mac_total") → 返回 N
       c) parseIntFromScript(html, "mac_page") * 20 → 返回总条数
       d) parseIntFromAttr(html, "data-total") → 返回 N
       e) countPageLinks(html) * 30（统计 <li class=...page...>N</li>
          和 ?page=N 出现的最大页数，每页估算 30 条）
       f) extractMaxPageNumber(html) * 30

  ④ body 为空或所有猜测均失败 → pagecount = 50（DEFAULT_UNKNOWN_PAGE_COUNT），
     total = 50 * size

  ⑤ result.limit = max(90, size)（单页条目数下限 90）

说明：parseTotalFromHtml 与 wrapList 内 parseTotalUniversal 的猜测逻辑完全一致，
两者互为冗余校验，取第一个有效值。

────────────────────────────────────────────────────────────────
2. categoryUrl 占位符完整替换链
────────────────────────────────────────────────────────────────
categoryUrl(tid, pg, filter, extend) 按顺序执行：

  ① stripBackticks(url)：循环去除首尾反引号 `

  ② ";;" 截断：url.contains(";;") 时截取 ";;" 之前的部分（用于双 URL 分离
     分类页与列表页的情况）

  ③ extend 参数替换（filter=true 且 extend 非空时）：
     对每个 key/value，执行：
       cateUrl = cateUrl.replace("{"+key+"}",
           URLEncoder.encode(value,"UTF-8").replace("+", "%20"))
     说明：空格编码为 %20 而非 "+"（避免 URL 中 + 被误解析为空格）

  ④ offset_paging 模式：
     pageSize = getRuleVal("page_size")，默认 20
     pageNum = shiftStartPage(pg) = parse(pg) + startpage - 1
     若 offset_paging="1" 且 url 不含 {offset}：
       pgVal = (pageNum-1)*pageSize（将页码转为偏移量）
     否则 pgVal = shiftStartPage(pg)（原页码）

  ⑤ 占位符替换：
     {offset} = (pageNum-1)*pageSize（与 offset_paging 无关始终计算）
     {limit}  = pageSize
     {cateId} = tid
     {catePg} = pgVal

  ⑥ KNOWN_BRACE_KEYS 过滤（L129-132）：
     集合包含：cateId, catePg, cateIdEn, class, area, by, year, lang, letter,
               page, pg, offset, limit
     对 url 中剩余的 {xxx}，若 xxx ∈ KNOWN_BRACE_KEYS：
       查找 url[lastEnd:] 中 "/" + xxx + "/" 子串，跳过该段
       （将如 /class/喜剧/ 这类筛选路径整体删除）
     最终返回清理后的 url

────────────────────────────────────────────────────────────────
3. 图片净化 resolveLazyImage
────────────────────────────────────────────────────────────────
当 img_html 为 <img ...> 标签时（非纯 URL）执行：

  ① Jsoup.parse(imgHtml).selectFirst("img") 取第一个 <img> 节点
  ② 若无 <img>，扫描 lazy-load 类属性：
       [data-original],[data-src],[data-lazy],[data-lazy-src],
       [data-original-src],[data-img],[exposuresrc],[_src],
       [lazy-src],[data-bg]
  ③ 先查 img_attr 配置的值（优先级最高），命中即返回
  ④ 再遍历 LAZY_IMG_ATTRS 顺序兜底
  ⑤ 最终返回 trim() 后的属性值，未命中则返回空串

LAZY_IMG_ATTRS 完整顺序（L180-183）：
  data-original → data-src → data-lazy → data-lazy-src → data-original-src
  → data-img → exposuresrc → _src → lazy-src → data-bg → src

────────────────────────────────────────────────────────────────
4. 搜索配置自动探测（searchContent 调用链）
────────────────────────────────────────────────────────────────
searchContent 在 search 规则缺失时执行：

  ① guessSearchUrlIfNeeded()：
     fetchOrCacheHomePageBody()（首页 HTML 缓存 HOME_CACHE_TTL_MS=5分钟）
     → guessSearchUrlUniversal(body)（探测 SEARCH_PATH_PROBES 中的路径）
     → guessRuleSearchUrl(body)（从首页链接猜测）
     → 若命中，构造 {url: "..."} 写入 rule.search

  ② 若仍无搜索规则：generateDefaultSearchConfig()：
     向 {homeUrl}/index.php/ajax/suggest?mid=1&wd=test 发起请求
     → 成功则构造：
       {
         "vod_id":"id", "vod_name":"name", "vod_pic":"pic",
         "url":"/index.php/ajax/suggest?mid=1&wd={wd}"
       }

  ③ applyFlatSearchFields()：扁平 search_url 字段 → search 子对象

  ④ initializeSearchConfig()：CSS/Jsoup 搜索规则初始化

  搜索词 URL 构建（buildSearchUrl，L6458-6465）：
    - search_word_sep 非空：keyword = keyword.replace(" ", sep)
    - {wd} = URLEncoder.encode(keyword, "UTF-8")
    - {pg} = shiftSearchPage(page) = parse(page) + sea_firstpage - 1
    - 末尾追加 search_suffix（若有）

  搜索处理优先级（searchContent L6341-6416）：
    searchjsonlist 显式指定 → JSON 解析
    → RuleUtils.getRegion(content, search) 截取
    → search_twice 二次截取
    → search_mode=0：JSON 优先（parseSearchResult → parseJsonSearchResult）
    → isCssModeEnabled：CSS 提取（失败回退）
    → parseHtmlSearchResults（search_mode=1 时 JSON 作为兜底）

────────────────────────────────────────────────────────────────
5. playlistContent 选集后处理完整链路
────────────────────────────────────────────────────────────────
playlistContent(ids, vod, body) 执行顺序（L5447-5520）：

  ① 解码 ids[0]（Base64）→ vinfo JSONObject

  ② playlist 规则处理：
     - playlist.url 非空且 != detail.url → fetchUrl(playlist.url) 获取播放页
     - RuleUtils.getRegion(body, playlist) 截取播放内容区

  ③ play_twice 二次截取（若有）

  ④ obtainPlayUrlList(content, playlist, vinfo) → vodPlayUrl
     findVodPlayFrom(content, size) → vodPlayFrom
     alignPlaySources(vodPlayFrom, vodPlayUrl) 对齐

  ⑤ epiurl_prefix / epiurl_suffix 前后缀拼接
     resolveEpiUrlVal 支持 {{变量}}；applyEpiUrlAdjust 批量应用

  ⑥ processEpisodes(vodPlayUrl)（L5522-5560）：
     - episode_dedup=1：同线路内按 url 去重（Set<String> seenUrl）
     - episode_quality：按 "蓝光>>超清>>高清" 优先级链 pickBestQuality
     - episode_sort=1：extractEpisodeNum 提取数字，全数字才排序

  ⑦ line_name_map：线路名重命名（applyLineNameMap）

  ⑧ empty_play_url 兜底（L5486-5497）：
     无集数时生成一条占位集数 "第1集$空播放URL"，线路名取 empty_play_from
     或默认"空播放"

  ⑨ reorderPlaySources：playlist.vod_play_from 数组控制线路顺序（L5808-5846）

  ⑩ merge_lines=1 且线路>1（L5501-5513）：
     合并为单条线路，$ 分隔集数，线路名取第一条原始名称

  ⑪ vod_play_url = TextUtils.join("$$$", vodPlayUrl)
     vod_play_from = TextUtils.join("$$$", vodPlayFrom)

────────────────────────────────────────────────────────────────
6. proxy 分发逻辑（proxy://do=XBPQ 入口）
────────────────────────────────────────────────────────────────
proxy(params) 分发顺序（L8255-8280）：

  ① do=XBPQ & danmu_url/danmuUrl → loadDanmu(params)（L8097-8136）
     - URLDecoder 解码
     - SSRF 拦截（isInternalUrl）
     - 白名单校验（isProxyOriginAllowed）
     - OkHttp.string(danmuUrl, headers) 拉取
     - jsonArray2xml 转 B站风格 XML；失败则原样返回 application/octet-stream

  ② do=XBPQ & m3u8 → loadM3u8(params)（L8182-8252）
     - 探测 base64 编码的 m3u8 内容（长度>200 且无 ://）
     - SSRF/白名单校验（非 base64 内容时）
     - 相对路径补全：base 优先，否则取自 URL 域名
     - #EXTM3U/#EXTINF 探测后同样走 m3u8 分支
     - 返回 application/vnd.apple.mpegurl

  ③ do=XBPQ & url（或 pic）→ loadPic(params)（L8043-8095）
     - URLDecoder 解码 pic 参数
     - SSRF 拦截 → 白名单校验 → MD5 签名校验（secretKey 非空时：MD5(pic+secretKey)）
     - OkHttp.proxy(pic, headers) 回源，返回原 Content-Type

  ④ url 参数含 .m3u8 或 base64 解码后含 #EXTM3U/#EXTINF → 走 loadM3u8

  ⑤ 所有回源强制 SSRF 防护，不受 allow_internal 影响

────────────────────────────────────────────────────────────────
7. playerContent 播放链完整优先级
────────────────────────────────────────────────────────────────
playerContent(flag, url, vipFlags) 执行顺序（L5906-5936，任一非 null/非空即返回）：

  ① tryForcePlay(url)（force_play=1/2，L5992-6022）：
     - webUrl = play_prefix + url + play_suffix
     - 含 #isVideo=true# 强制标记视频
     - isVideoFormat 或 isThunder → parse=0 直出
     - isVipPlayUrl（内置 Vip 域名表 + 框架透传 vipFlags）→ parse=1, jx=1
     - 否则 → parse=1 嗅探
     - applyPlayHeader 注入 play_header JSON

  ② tryMacPlayer(html)（Anal_MacPlayer=1/2 或 auto_maccms=1/2，L6040起）：
     - P_PLAYER_OBJ 正则提取 var player_xxx = {...}
     - Anal_MacPlayer=2：正则再解析 MacPlayer 脚本块提取真实 URL
     - tryDecryptParsedUrl 解密（encrypt 字段）

  ③ parsePlayUrl(flag, url, html, vipFlags)（L5847-5886）：
     - RuleUtils.getRegion(html, play) 截取 play 区域
     - lookback 回退定位起始位置
     - RuleUtils.findSubString 取 vod_url 规则结果
     - hexEscapeDecode 解码 JS 转义
     - tryDecryptParsedUrl 解密
     - isDirectLinkUniversal 或 isVideoFormat 校验通过后 → parse=0 直出

  ④ tryJumpUrl(url, html)（jump_url 非空，L6100-6147）：
     - extractJumpUrl：startFlag 含 * → player 对象提取 JSON.url；
       否则 && 截取
     - tryDecryptParsedUrl 解密
     - 相对路径 → resolveRedirectTarget 补全
     - isVideoFormat/isVipPlayUrl 判定 → parse=0 或 parse=1 jx=1

  ⑤ tryScanVideoLink(url, html)（兜底，L6188起）：
     - 策略1：P_PLAYER_OBJ 直接提取播放器对象（无需 Anal_MacPlayer 配置）
     - 策略2：P_VIDEO_DIRECT 正则扫描 .m3u8/.mp4 直链
     - 失败则返回 null

  ⑥ buildSniffResult(url)：parse=1，player 自行嗅探

  所有返回结果均经 appendDanmuParam 注入弹幕参数（danmuUrl 非空时）

────────────────────────────────────────────────────────────────
8. 多线模式触发与执行
────────────────────────────────────────────────────────────────
触发条件（两者同时配置且非空，L4667-4682）：
  multi_line_array && multi_line_url

执行流程（tryMultiLineMode）：
  ① multi_line_twice 二次截取（若有）
  ② extractMultiLines(body, arrayRule, urlRule, prefix, suffix)：
     - arrayParts = applyOrSelector(arrayRule).split("&&")
     - urlParts = applyOrSelector(urlRule).split("&&")
     - 循环最多 10 条：找 arrayStart..arrayEnd 区间，再在其中
       找 urlStart..urlEnd，拼接 prefix+content+suffix
  ③ 返回 List<String> 单元素（"集1#集2#..." 格式）

tryFromLinkMode 补充（L4716-4755）：
  - 仅当 multi_line_array 为空时才走此模式
  - 对每条线路独立 fetchUrl 播放页，再 extractVideoListByLink
  - 线路上限 10 条

────────────────────────────────────────────────────────────────
9. 常量与边界值
────────────────────────────────────────────────────────────────
  DEFAULT_HOME_MAX_VIDEOS = 20        首页每分类最多聚合条数
  MAX_PAGE_ITEMS = 100                单页列表最大条目数（超限截断）
  DEFAULT_UNKNOWN_PAGE_COUNT = 50     无法推测总页数时的兜底页码
  MAX_HTML_LENGTH = 2MB               首页缓存最大长度（超长截断到最近 > 位置）
  HOME_CACHE_TTL_MS = 5 * 60 * 1000   首页 HTML 缓存时间（5分钟）
  variableMap 递归展开上限：10 轮

================================================================================
六、特殊功能说明
================================================================================

────────────────────────────────────────────────────────────────
1. 反爬绕过（Cloudflare / 宝塔 WAF）
────────────────────────────────────────────────────────────────
- Cloudflare：isCloudflarePage 检测挑战页（cf-challenge/cf-browser-verification
  等特征）→ bypassCloudflare 提取 cf_clearance cookie 携带重试
- 宝塔 WAF：isBaoTaWafPage 检测（btwaf/检测中/安全检测 等特征）→
  extractBtwafToken 提取 token 生成 cookie 重试
- antiCrawlTimeout 控制反爬重试总预算；Cookie 经 Util.mergeCookies 合并回写

────────────────────────────────────────────────────────────────
2. 图片净化与代理
────────────────────────────────────────────────────────────────
- img_attr 指定懒加载真实地址属性（data-original 等）
- default_pic 默认封面兜底
- PicNeedProxy=1 生成 proxy://pic=URL&key=MD5(pic+secretKey)，
  loadPic 回源校验签名；pic_allow_domains 白名单限定代理域名

────────────────────────────────────────────────────────────────
3. SSRF 防护（isInternalUrl）
────────────────────────────────────────────────────────────────
拦截：
- 危险协议：file/gopher/dict/ftp/jar/netdoc/javascript/mailto
- 内网 IP：127./10./172.16-31./192.168./169.254./0.0.0.0
- localhost / .local / .internal / .localhost
- IPv6：::1 / ULA(fc/fd) / 链路本地(fe80)
proxy() 入口强制拦截，不受 allow_internal 影响。
P2P 链接（magnet/thunder/ed2k/qqdl/btih/urn:btih）直接交给播放器，不走 HTTP。

────────────────────────────────────────────────────────────────
4. 动态域名（域名-c / 主页url-c）
────────────────────────────────────────────────────────────────
applyDynamicDomain()（规则加载时执行）：
  1) 域名-c 展开 {{变量}} + [替换:] 后处理器 → isValidOrigin 校验
  2) 有效则把整份规则中旧 homeUrl 的 origin 全局替换为新 origin（域名切换）
  3) 同时填充 {{域名-c}}/{{主页url-c}} 变量
说明：仅 域名-c 有效时触发切换；主页url-c 只作变量来源。

────────────────────────────────────────────────────────────────
5. 镜像域名与全局参数
────────────────────────────────────────────────────────────────
- mirror_hosts：站点请求失败（5xx/超时）时逐个切换镜像域名重试
- url_append：追加到每次请求 URL 的固定参数（如 "token=xxx"）

────────────────────────────────────────────────────────────────
6. 选集链接前后缀（选集链接加前缀/加后缀）
────────────────────────────────────────────────────────────────
播放线路提取阶段对每条集数链接自动拼接前缀/后缀。
适用于选集链接需要拼接固定路径的场景（如 "/play/" + id + "/1.m3u8"）。

────────────────────────────────────────────────────────────────
7. 编码与解码
────────────────────────────────────────────────────────────────
- encoding：GBK/GB2312 站点响应强制矫正
- hexEscapeDecode：链接中 %uXXXX JS 转义解码（详情链接）
- convertUnicodeToChinese：响应 \uXXXX 转义解码

================================================================================
六、注意事项
================================================================================

1. 配置值约定：留空 / "空" / "&&" 均视为未配置；布尔用 "0"/"1"。
2. 键归一化：XBPQKey.norm 大小写不敏感；未登记键原样透传不报错。
3. 直接播放与详情页互斥：force_play=1/2 时线路/播放解析配置不生效。
4. 多线模式触发：multi_line_array + multi_line_url 同时配置且非空时生效。
5. 线路合并行为：合并后线路名取第一条线路原始名称（非默认"线路"）。
6. 热门推荐：hot_recommend=1 时无 首页 配置也能聚合主页视频。
7. 图片代理密钥泄露则签名失效，请勿公开传播 代理密钥。
8. 单条解析失败不影响整体：各方法均有独立 try-catch，错误仅打日志。
9. bad item 默认跳过：skip_bad_item=1 单条失败剔除该条；"0"=整页失败。
10. vod_id 为 Base64 编码 JSON（encodeVodId），detailContent 自动解码，
    规则书写者无需关心。
11. 视频过滤词优先级高于嗅探词，命中强制判定为非视频。
12. WebView 渲染四键仅注册未实现，运行时输出告警并忽略。
13. 特殊分类链接 / 直播url 仅注册，引擎不消费。

================================================================================
七、版本信息
================================================================================

源文件：com.github.catvod.spider.XBPQ.java + XBPQKey.java（单文件实现 + 键注册表）
依赖：OkHttp / Jsoup / org.json / Spider 基类 / Util（mergeCookies/isThunder）
特性：中英文双键（XBPQKey 统一登记）/ && 正则截取 / CSS 选择器 / 多线聚合 /
      Cloudflare+宝塔反爬 / 图片代理 / 弹幕加载 / M3U8 解析 / {{变量}} 模板 /
      动态域名切换 / 镜像域名 / SSRF 防护 / 选集链接前后缀 /
      分类/列表/搜索 JSON 模式 / 自动猜测链路
测试：xbtest/ 桌面验证环境，五套件 379 项断言 + RuleLint 95 个真实规则审计
